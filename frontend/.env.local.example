# AIRRA Frontend Environment Variables

# Backend API URL - SERVER SIDE ONLY (not exposed to browser)
BACKEND_API_URL=http://localhost:8000

# Backend API Key - SERVER SIDE ONLY (not exposed to browser)
# Get this from your backend .env AIRRA_API_KEY
BACKEND_API_KEY=dev-test-key-12345

# DEPRECATED - For backwards compatibility during migration
# SECURITY WARNING: NEXT_PUBLIC_* variables are exposed in browser!
# Use BACKEND_API_KEY instead for production
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_KEY=dev-test-key-12345
